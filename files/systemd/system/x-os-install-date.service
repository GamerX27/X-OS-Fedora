[Unit]
Description=Record the first-boot install date
ConditionPathExists=!/var/lib/x-os/install-date

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/sh -c 'mkdir -p /var/lib/x-os && date -u +%%Y-%%m-%%d > /var/lib/x-os/install-date'

[Install]
WantedBy=multi-user.target
